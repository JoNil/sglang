--- a/gemm_base.py
+++ b/gemm_base.py
@@ -53,6 +53,7 @@ from ..utils import (
     get_native_fp4_dtype,
     is_sm100a_supported,
     is_sm100f_supported,
+    is_sm110a_supported,
     is_sm12x_supported,
     LibraryError,
     backend_requirement,
@@ -6201,7 +6202,7 @@ def bmm_fp8(
     return out
 
 
-@supported_compute_capability([100, 103, 120, 121])
+@supported_compute_capability([100, 103, 110, 120, 121])
 def _cutlass_gemm_fp8_nt_groupwise_requirement(
     a: torch.Tensor,
     b: torch.Tensor,
@@ -6387,7 +6388,7 @@ def gemm_fp8_nt_groupwise(
                 *scale_granularity_mnk,
                 scale_major_mode,
             )
-        elif is_sm100a_supported(a.device):
+        elif is_sm100a_supported(a.device) or is_sm110a_supported(a.device):
             get_gemm_sm100_module().gemm_fp8_nt_groupwise(
                 workspace_buffer,
                 a,
@@ -6658,7 +6659,7 @@ def gemm_fp8_blockscale(
     )
 
 
-@supported_compute_capability([100, 103, 120, 121])
+@supported_compute_capability([100, 103, 110, 120, 121])
 def _check_group_gemm_fp8_nt_groupwise_problem_size(
     a: torch.Tensor,
     b: torch.Tensor,
@@ -6832,7 +6833,7 @@ def group_gemm_fp8_nt_groupwise(
             *scale_granularity_mnk,
             scale_major_mode,
         )
-    elif is_sm100a_supported(a.device):
+    elif is_sm100a_supported(a.device) or is_sm110a_supported(a.device):
         get_gemm_sm100_module().group_gemm_fp8_nt_groupwise(
             int_workspace_buffer,
             float_workspace_buffer,
@@ -7063,7 +7064,7 @@ def group_gemm_mxfp8_mxfp4_nt_groupwise(
             tile_k,
             swap_ab,
         )
-    elif is_sm100a_supported(a.device):
+    elif is_sm100a_supported(a.device) or is_sm110a_supported(a.device):
         get_gemm_sm100_module().group_gemm_mxfp4_nt_groupwise(
             int_workspace_buffer,
             float_workspace_buffer,
